const bottomCircleDisplay = () => ({
    init() {
      const nextButton = document.getElementById('bottomCircleButton');
      nextButton.style.display = 'block';
    },
  })
  
  export {bottomCircleDisplay}